# 测试资产规划

本项目测试不以“最后补几条接口测试”为目标，而是按多层验证建设：

- `unit/`：单位换算、年龄换算、派生指标、规则引擎
- `contract/`：FastAPI 请求响应与状态流转契约
- `integration/`：上传、脱敏、OCR、校验、人工审核、入库全链路
- `e2e/`：前端上传、审核、趋势图、报警展示
- `fixtures/`：结构化样本与匿名化测试数据

建议后续按 `docs/specs/TEST_STRATEGY.md` 建立正式测试目录。
