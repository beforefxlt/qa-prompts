# 后端模块占位

本目录预留给 `FastAPI` 服务实现。

建议后续目录：
- `app/api/`：路由与 Schema
- `app/domain/`：领域模型
- `app/services/`：OCR 编排、脱敏、趋势分析
- `app/rules/`：单位与逻辑校验规则引擎
- `app/workers/`：异步任务
- `tests/`：单元测试、集成测试、合约测试

当前阶段只完成项目初始化，不放置正式业务代码。
